# Copyright 1999-2015 Gentoo Foundation
# Distributed under the terms of the GNU General Public License v2
# $Header: $

EAPI=5

# ebuild generated by hackport 0.4.5

CABAL_FEATURES="bin lib profile haddock hoogle hscolour"
inherit haskell-cabal

MY_PN="SHA"
MY_P="${MY_PN}-${PV}"

DESCRIPTION="Implementations of the SHA suite of message digest functions"
HOMEPAGE="http://hackage.haskell.org/package/SHA"
SRC_URI="mirror://hackage/packages/archive/${MY_PN}/${PV}/${MY_P}.tar.gz"

LICENSE="BSD"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE="+decoderinterface exe test"

RDEPEND=">=dev-lang/ghc-7.4.1:=
	test? ( >=dev-haskell/quickcheck-2.5:2=[profile?] <dev-haskell/quickcheck-3:2=[profile?]
		>=dev-haskell/test-framework-0.8.0.3:=[profile?] <dev-haskell/test-framework-10000:=[profile?]
		>=dev-haskell/test-framework-quickcheck2-0.3.0.2:=[profile?] <dev-haskell/test-framework-quickcheck2-10000:=[profile?] )
	decoderinterface? ( >=dev-haskell/binary-0.7:=[profile?] <dev-haskell/binary-10000:=[profile?] )
	!decoderinterface? ( >=dev-haskell/binary-0:=[profile?] <dev-haskell/binary-0.7:=[profile?] )
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-1.8
"

S="${WORKDIR}/${MY_P}"

src_configure() {
	haskell-cabal_src_configure \
		$(cabal_flag decoderinterface decoderinterface) \
		$(cabal_flag exe exe) \
		$(cabal_flag test test)
}
